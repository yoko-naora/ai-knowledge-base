# HERMES-RULES.md — Hermes 强制规则

> **违反任一条 → 停止工作 → 报告 Claude。不可绕过。**

---

## 🔴 开工检查（每次任务前必须完成，5 条全 Yes 才能写代码）

| # | 检查项 | 怎么做 |
|---|--------|--------|
| 1 | Skill 存在吗？ | `Skill` 工具加载，返回了内容 = 存在。返回空/报错 = 不存在 |
| 2 | Skill 加载成功吗？ | 读到了 Skill 的实际指令文字，不是只看了名字 |
| 3 | 项目文档读了吗？ | 按任务指定的顺序读完所有前置文件 |
| 4 | 任务边界清楚吗？ | 只做自己的职责，不跨到别的 Agent 的活 |
| 5 | 能做到吗？ | 5 条都 Yes 才开始。任一 No → 停 |

---

## 🔴 Skill 铁律

```
Skill 不存在 → 立即停止 → 报告
Skill 加载失败 → 立即停止 → 报告
Skill 加载成功但内容看不懂 → 立即停止 → 问 Claude
```

**绝对禁止：** 凭 Skill 名字猜测它干什么、凭描述硬写、跳过 Skill 直接写代码、加载失败后假装用了。

---

## 🔴 硬写禁令

以下行为视为违规：

- Skill 不存在时，自己脑补 Skill 的效果硬写代码
- 加载了 Skill 但不按其流程走，直接跳到写代码
- 输出和其他版本"看起来一样"的产物（说明没按 Skill 的差异化逻辑做）
- 用"品牌约束太紧所以都一样"当借口

---

## 🔴 交付标准

每个版本完成后，文件顶部必须注释：

```html
<!-- Skill: [skill名] | 加载: [成功/失败] | 遵循Skill流程: [是/否] | 本版本独特之处: [一句话] -->
```

如果"加载"或"遵循Skill流程"是"否" → 这个版本无效，不要提交。

---

## 🟢 违规处理

```
违规 → 停止 → 说清楚哪里违规 → 等 Claude 决策（继续/重做/换人）
```

不猜、不混、不装。
