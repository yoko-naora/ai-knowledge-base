# HERMES-RULES.md — Hermes 强制规则

> **违反任一条 → 停止工作 → 报告 Claude。不可绕过。**

---

## 🔴 开工检查（用户说「开工」时逐条执行，全部 Yes 才能写代码）

| # | 检查项 | 怎么做 |
|---|--------|--------|
| **0** | **SESSION.md 读了？** | Read `_context/SESSION.md`。恢复上下文。跳过 = 不知道项目状态 |
| **1** | **git log 看了？** | Bash `git log -3 --oneline`。知道最近改了什么 |
| **2** | **分级判断做了？** | 新功能/支付/安全 → 完整开工（再读 PROJECTS.md + PROJECT.md）。文案/样式/错字 → 快速开工 |
| **3** | **全项目简报出了？** | 每个项目状态 + 最优先任务。简报后才问「做什么」 |
| **4** | **preflight 跑了？** | `bash scripts/preflight.sh` → `[✓]` 才过。没跑 = 没开工 |
| **5** | Skill 存在？ | `Skill` 工具加载，返回了内容 = 存在。空/报错 = 不存在 |
| **6** | Skill 加载成功？ | 读到了 Skill 的实际指令文字，不是只看了名字 |
| **7** | 项目文档读了？ | 按任务指定的顺序读完所有前置文件 |
| **8** | 任务边界清楚？ | 只做自己的职责，不跨到别的 Agent 的活 |
| **9** | 能做到？ | 10 条都 Yes 才开始。任一 No → 停 |

> 🔴 10 条全 Yes 才能写代码。任一跳过 → 违规 → 停止 → 报告 Claude。

---

## 🔴 收工协议

收工时**只由被说话的那一端执行**。更新 SESSION.md + commit + 简报 + 交叉检查手渡し。未被告知收工 → 不出手。

---

## 🔴 Skill 铁律

```
Skill 不存在 → 立即停止 → 报告
Skill 加载失败 → 立即停止 → 报告
Skill 加载成功但内容看不懂 → 立即停止 → 问 Claude
```

**绝对禁止：** 凭 Skill 名字猜测它干什么、凭描述硬写、跳过 Skill 直接写代码、加载失败后假装用了。

---

## 🔴 硬写禁令

以下行为视为违规：

- Skill 不存在时，自己脑补 Skill 的效果硬写代码
- 加载了 Skill 但不按其流程走，直接跳到写代码
- 输出和其他版本"看起来一样"的产物（说明没按 Skill 的差异化逻辑做）
- 用"品牌约束太紧所以都一样"当借口

---

## 🔴 交付标准

每个版本完成后，文件顶部必须注释：

```html
<!-- Skill: [skill名] | 加载: [成功/失败] | 遵循Skill流程: [是/否] | 本版本独特之处: [一句话] -->
```

如果"加载"或"遵循Skill流程"是"否" → 这个版本无效，不要提交。

---

## 🟢 违规处理

```
违规 → 停止 → 说清楚哪里违规 → 等 Claude 决策（继续/重做/换人）
```

不猜、不混、不装。
