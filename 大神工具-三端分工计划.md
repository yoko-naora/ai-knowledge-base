# 大神工具 · 三端分工计划

**版本:** v1  
**日期:** 2026-06-12  
**依据:** 大神工具-设计文档.md v2  

---

## 一、总览

```
8 步工作流        Claude Code      Hermes          Codex
──────────────────────────────────────────────────────────
Step 1-2 第一稿    API × 3          generator.html    prompt 提取
Step 3-5 加料      —                UI 交互            —
Step 6 质检        API × 1          报告展示           prompt 提取
Step 7 配图        API × 1          推荐+预览          分类映射
Step 8 封面        API × 1          封面展示           —
──────────────────────────────────────────────────────────
交叉检查           —                —                最终报告
```

---

## 二、Claude Code · 后端 API

### 职责
5 个 Cloudflare Function endpoint，每个对应一个 skill 的逻辑。

### 工作内容

| # | 文件 | 功能 | 基于 |
|---|------|------|------|
| 1 | `functions/api/writing-fragments.js` | 审题 | writing-fragments SKILL.md |
| 2 | `functions/api/writing-shape.js` | 写正文 | writing-shape SKILL.md |
| 3 | `functions/api/dbs-content.js` | 五维诊断 | dbs-content SKILL.md |
| 4 | `functions/api/recommend-prompts.js` | 配图推荐 | prompts/data.json 分类映射 |
| 5 | `functions/api/generate-cover.js` | 封面生成 | _blocks/baoyu/cover-presets/ |

### 技术约束
- 用 `context.env.XXX` 取密钥，**不能有默认值**
- POST 方法，JSON 输入输出
- 遵循现有 `functions/api/*.js` 的写法（参考 stripe-webhook.js）
- 编辑 `functions/api/*.js`，**不是** `_legacy/netlify/functions/*.mjs`
- 模型建议：writing-shape 用 Sonnet，其余用 Haiku
- LLM API key 放 Cloudflare 环境变量

### 接口契约（Hermes 依赖这些格式）

#### POST /api/writing-fragments
```js
// 输入
{ topic: string, platform: "小红书"|"公众号" }
// 输出
{ angles: string[], gaps: string[] }
```

#### POST /api/writing-shape
```js
// 输入
{ topic: string, platform: string, angles: string[], addons?: string[], userCustomText?: string }
// 输出
{ title: string, body: string }
```

#### POST /api/dbs-content
```js
// 输入
{ article: { title: string, body: string } }
// 输出
{ textCleanliness: { grade: "✅"|"⚠️"|"❌", detail: string },
  headlineDiagnosis: { grade, detail },
  expressionEfficiency: { grade, detail },
  cognitiveGap: { grade, detail },
  aiSuggestions: string }
```

#### POST /api/recommend-prompts
```js
// 输入
{ topic: string, category?: string }
// 输出
{ recommended: [{ cat: string, prompts: [{ id: number, title: string, image_prompt: string }] }],
  allCategories: string[] }
```

#### POST /api/generate-cover
```js
// 输入
{ article: { title: string, body: string }, platform: "xiaohongshu"|"video" }
// 输出
{ imageUrl: string }
```

### 启动顺序
- **Phase 1**（与 Hermes 并行）：
  1. 读 prompts/data.json，了解分类结构
  2. 读 _blocks/STYLE-GUIDE.md，了解视觉路由规则
  3. 等 Codex 交付 prompt 模板后开始编码
- **Phase 2**：写 5 个 endpoint，逐个 `curl` 验证
- **Phase 3**：与 Hermes 联调

---

## 三、Hermes · 前端 generator.html

### 职责
单 HTML 文件 `generator.html`，实现 8 步状态机全部 UI。

### 工作内容
1. 新建 `generator.html`（参考 creator.html 的结构）
2. 8 步 UI 组件：
   - Step 1: 主题输入 + 平台选择
   - Step 2: 第一稿展示
   - Step 3: 加料面板（勾选项 + 诊断驱动的推荐语）
   - Step 4-5: 第二稿 + diff 高亮（蓝色标新增）
   - Step 6: 五维诊断报告展示（✅/⚠️/❌ 每项可点修复）
   - Step 7: 配图推荐 + 预览（小红书 3:4 + 视频号 9:16）
   - Step 8: 封面展示
3. 状态管理：localStorage + 流程控制
4. 调后端 API（开发阶段用 mock 数据，联调阶段接真实 API）

### 技术约束
- **先读 `_blocks/STYLE-GUIDE.md`**，确定视觉风格路由
- 纯 vanilla HTML/CSS/JS，单文件
- 沿用设计系统：Noto Sans/Serif JP + 金棕配色
- 字号体系、间距与 creator.html 一致
- **不能手写 HTML/CSS 替代种子模板**（guizang 类卡片必须拷模板）
- **不能跳过设计流程** — 只要有 layout/色彩/组件结构/视觉层级改动，先走 huashu-design
- **emoji 禁止出现在任何视觉产出中**
- 状态机状态：`input → drafting → draft1 → addons → draft2 → qc → image → cover → done`

### Mock 数据（开发阶段用）
等不到 real API 时，先在 `generator.html` 硬编码测试数据：
```js
const MOCK = {
  fragments: { angles: ["角度1", "角度2", "角度3"], gaps: ["缺案例", "缺数据"] },
  draft1: { title: "测试标题", body: "测试正文段落..." },
  diagnosis: { textCleanliness: { grade: "✅", detail: "文字干净" }, ... },
  prompts: { recommended: [...], allCategories: [...] },
  imageUrl: "https://placehold.co/360x480"
}
```

### 启动顺序
- **Phase 1**（与 Claude Code 并行）：
  1. 读 _blocks/STYLE-GUIDE.md
  2. 建 generator.html 骨架 + 状态机 + 所有 UI 组件
  3. 用 mock 数据走通 8 步
- **Phase 2**：接真实 API，替换 mock
- **Phase 3**：与 Claude Code 联调

---

## 四、Codex · 内容 + 测试

### 职责
3 份 prompt 模板 + 分类映射逻辑 + 终稿交叉检查。

### 工作内容

#### 4.1 Prompt 提取（Phase 1）
从 3 个 SKILL.md 提取 LLM 可用的 system prompt + user prompt 模板：

| Skill | 文件位置 | 输出 |
|-------|---------|------|
| writing-fragments | .agents/skills/dbs/skills/writing-fragments/SKILL.md | system_prompt + user_template |
| writing-shape | .agents/skills/dbs/skills/writing-shape/SKILL.md | system_prompt + user_template |
| dbs-content | .agents/skills/dbs-content/SKILL.md | system_prompt + user_template |

输出格式（供 Claude Code 直接使用）：
```json
{
  "system_prompt": "你是一个内容审题助手...",
  "user_template": "主题：{topic}\n平台：{platform}",
  "response_format": "JSON"
}
```

#### 4.2 分类映射逻辑（Phase 1-2）
写一个 `topic → prompts 分类` 的匹配函数：
- 输入：topic（如"夏季口红推荐"）
- 输出：推荐 2 个分类（如"电商/商业海报" + "穿搭/形象/造型"）
- 可基于关键词匹配，不需要 LLM
- 输出位置：`functions/api/lib/category-mapping.js`（给 Claude Code 用）

#### 4.3 交叉检查（Phase 3）
全流程跑通后，按 crosscheck-report.md 模式输出交叉检查报告：
| 检查项 | 覆盖内容 |
|--------|---------|
| API 端点 | 每个 endpoint 的输入/输出正确性 |
| 前端状态机 | 8 步流转无断点 |
| 数据一致性 | localStorage ↔ API 数据格式匹配 |
| 视觉一致性 | 与设计系统匹配 |
| 错误处理 | API 超时/失败/空数据时前端表现 |

### 输出交付
- `prompts/api-templates/` 目录下 3 个 JSON prompt 模板文件
- `functions/api/lib/category-mapping.js`
- 最终 `crosscheck-report-大神工具.md`

---

## 五、整合流程

### 依赖关系

```
Phase 1 (并行)
  Codex: 提取 prompt 模板 ──────────────────┐
  Hermes: generator.html 骨架 + mock 数据    │
                                            │
Phase 2 (Codex 交付后)                       │
  Codex: 分类映射逻辑 ──────┐                │
                           ▼                ▼
  Claude Code: 用 prompt 模板写 5 个 API endpoint
                                            │
Phase 3 (API 就绪后)                         │
  Hermes: 替换 mock → 接真实 API ←──────────┘
  Codex: 交叉检查报告
```

### 接口契约文件
所有 agent 开工前读：`大神工具-设计文档.md`
所有 agent 联调时参照：**二、接口契约**（本文件）

### 提交规则（照 CLAUDE.md）
```bash
git add -A && git commit -m "[AgentName] <what>" && git push
```
- 永远不 `git push --force`
- 开工前 `git pull`
- 废弃文件走 Dead File Protocol（移 `_legacy/` + 2 周过期）

### 工作时间窗
| Agent | Phase 1 | Phase 2 | Phase 3 |
|-------|---------|---------|---------|
| Codex | prompt 提取 | 分类映射 + 等交付 | 交叉检查 |
| Claude Code | 读设计文档 + 等 prompt | 写 API | 联调 |
| Hermes | UI 骨架 + mock | 等 API | 接 API + 联调 |

---

## 六、交叉测试清单

### 6.1 单 endpoint 测试（Claude Code 自测，用 curl）
```bash
# 每个 endpoint 验证：
curl -X POST "https://kb.snsaladdin.com/api/writing-fragments" \
  -H "Content-Type: application/json" \
  -d '{"topic":"夏季口红推荐","platform":"小红书"}'
```
- ✅ 正常输入返回 200 + 正确 JSON
- ✅ 空输入返回 400
- ✅ LLM 超时 → 500 + 用户友好消息
- ✅ 密钥缺失 → 500，不暴露具体错误

### 6.2 状态机流转测试（Hermes 自测）
| 测试 | 操作 | 预期 |
|------|------|------|
| 正常 8 步 | 全程走完 | 每一步 UI 正确显示 |
| Step 3 不勾直接"生成第二稿" | 不选加料点按钮 | 第二稿 = 第一稿（无新增） |
| Step 6 点某个 ⚠️ 修复 | 点"封面/标题 → 优化" | 标题被 AI 重写，蓝色高亮 |
| Step 6 点"60分跳过" | 跳过按钮 | 直接进 Step 7 |
| Step 7 点"不满意" | 不满意按钮 | 显示 prompt，不出第二张图 |
| Step 7 → Step 8 | 满意后进封面 | 正确展示封面结果 |
| 返回修改 | 任一 Step 点"返回" | 保留之前的数据 |

### 6.3 端到端测试（Hermes + Claude Code 联调）
| 测试 | 条件 | 预期 |
|------|------|------|
| 全流程 API 集成 | 接真实 API | 8 步走完，无断点 |
| API 超时 | 关闭网络 | 前端显示"加载中"→ 超时提示 |
| API 返回空数据 | mock 空数组 | 前端显示"暂无推荐" |
| 刷新页面 | 任意步骤 F5 | localStorage 恢复当前状态 |

### 6.4 交叉检查（Codex 终稿）
- 格式参照 `crosscheck-report.md`（满分 24 项，逐条 ✅/⚠️/❌）
- 额外检查大神工具独有的项目：
  - 8 步状态机无死循环
  - 免费配图配额控制正确（仅 1 次）
  - 不满意度复制 prompt 功能正常
  - 小红书 + 视频号尺寸同时输出
  - 蓝标高亮与普通正文能区分

---

## 七、文件清单（终稿时应存在）

```bash
kb-site/
├── 大神工具-设计文档.md          # 设计文档（已有）
├── 大神工具-三端分工计划.md       # 本文件（新建）
├── generator.html                # 前端（Hermes 负责）
├── functions/api/
│   ├── writing-fragments.js      # Claude Code
│   ├── writing-shape.js          # Claude Code
│   ├── dbs-content.js            # Claude Code
│   ├── recommend-prompts.js      # Claude Code
│   ├── generate-cover.js         # Claude Code
│   └── lib/category-mapping.js   # Codex
├── prompts/api-templates/
│   ├── writing-fragments.json    # Codex
│   ├── writing-shape.json        # Codex
│   └── dbs-content.json          # Codex
└── crosscheck-report-大神工具.md  # Codex（终稿）
```
